import React from 'react'
import { Button, Input } from './index'

const FormInput = props => {
  return (
    <div>
      <Button></Button>
      <Input />
    </div>
  )
}

export default FormInput
